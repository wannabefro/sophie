import Ember from 'ember';

export default Ember.ObjectController.extend({
  colors: ['red', 'pink', 'black', 'green', 'blue', 'orange', 'yellow'],
  characters: ['princess', 'prince'],
  pets: ['bird', 'cat', 'dog']
});
